# ---------------------------------------------------------------------------
# DO NOT COMMIT REAL SECRETS.
#
# This file is a TEMPLATE. The actual Secret must be created out-of-band, from
# Azure Key Vault, an Azure DevOps variable group (linked to Key Vault), or
# `kubectl create secret generic` in a controlled environment.
#
# Example (do NOT run with these placeholder values):
#   kubectl -n ksgaal create secret generic ksgaal-activity-map-secrets \
#     --from-literal=DATABASE_URL='postgresql://...' \
#     --from-literal=JWT_SECRET='...' \
#     --from-literal=SMTP_USER='...' \
#     --from-literal=SMTP_PASS='...' \
#     --from-literal=ADMIN_EMAIL='...' \
#     --from-literal=ADMIN_PASSWORD='...' \
#     --from-literal=ADMIN_NAME='Admin'
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: ksgaal-activity-map-secrets
  namespace: ksgaal
  labels:
    app.kubernetes.io/name: ksgaal-activity-map
    app.kubernetes.io/part-of: ksgaal-activity-map
type: Opaque
stringData:
  DATABASE_URL: "postgresql://USER:PASSWORD@HOST:5432/DATABASE"
  JWT_SECRET: "REPLACE_WITH_LONG_RANDOM_STRING_AT_LEAST_32_CHARS"
  SMTP_USER: "no-reply@example.com"
  SMTP_PASS: "REPLACE_ME"
  ADMIN_EMAIL: "admin@example.com"
  ADMIN_PASSWORD: "REPLACE_ME"
  ADMIN_NAME: "Admin"
