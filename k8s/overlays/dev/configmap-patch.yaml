apiVersion: v1
kind: ConfigMap
metadata:
  name: ksgaal-activity-map-config
# The only genuinely per-environment value that is not a setting someone
# tunes: which environment this is. Everything else comes from the aatw-dev
# variable group by way of the Secret.
data:
  APP_ENV: "dev"
