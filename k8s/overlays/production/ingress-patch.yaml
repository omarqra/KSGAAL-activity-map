apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ksgaal-activity-map
# Replaces the entire tls + rules block from the base Ingress.
# Update example.com with the real production hostname.
spec:
  tls:
    - hosts:
        - example.com
      secretName: ksgaal-production-tls
  rules:
    - host: example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ksgaal-activity-map
                port:
                  number: 80
