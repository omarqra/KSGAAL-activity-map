apiVersion: v1
kind: ConfigMap
metadata:
  name: ksgaal-activity-map-config
# Production-specific overrides for the base ConfigMap.
# Replace the placeholder URLs with real production hostnames before deploying.
data:
  APP_ENV: "prod"
  SMTP_HOST: "smtp.example.com"
  EMAIL_FROM: "KSGAAL <no-reply@example.com>"
  APP_URL: "https://example.com"
  NEXT_PUBLIC_FRONTEND_URL: "https://example.com"
  NEXT_PUBLIC_BACKEND_URL: "https://example.com"
  NEXT_PUBLIC_GOOGLE_MAPS_API_KEY: ""
