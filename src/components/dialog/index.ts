// Confirm Dialog Context (Provider + hook)
export {
  ConfirmDialogProvider,
  useConfirmDialog,
} from "./context/confirm-dialog-context";

// Base Dialog components
export { BaseDialog } from "./base/base-dialog";
export { DialogWithHook } from "./base/dialog-with-hook";
export { useBaseDialog } from "./base/use-base-dialog";

// Confirm Dialog components
export { ConfirmDialog, type ConfirmType } from "./functional/confirm-dialog";
